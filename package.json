{
  "name": "wispbyte-project",
  "version": "0.0.1",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "bun run ./scripts/build.ts"
  },
  "dependencies": {
    "@elysiajs/openapi": "^1.4.14",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.9.3"
  }
}
